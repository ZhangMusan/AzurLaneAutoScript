# 船坞扫描 - 舰娘名称图片处理流程 (OCR前)

位置：`module/retire/dock_scan_scanner.py` 中的 `NameScanner` 类

## 1. 图像补正 - `_match_name_band()`

目的：从舰娘卡片中精确定位名称所在的水平条带

**处理流程：**
```
1. 将RGB彩色图转换为灰度图
2. 创建一个高斯分布的权重曲线（弓形）
   - 高度取min(26, 图片高度)
   - 权重分布：[0.35, 0.55, 0.8, 1.0, 1.0, 1.0, 0.8, 0.55, 0.35]
3. 计算每行像素的"暗度" = 255.0 - 行平均值
4. 用权重点积暗度，找出分数最高的top位置
5. 返回定位后的名称文字条带 [top:bottom, :]
```

## 2. 二值化处理 - `_build_letter_binary()`

目的：为文字检测创建最佳的二值化图像

**处理方法：**

| 方法 | 参数 | 说明 |
|------|------|------|
| OTSU二值化 | `THRESH_BINARY + THRESH_OTSU` | 自适应全局阈值，处理整体对比度 |
| 自适应二值化 | `ADAPTIVE_THRESH_GAUSSIAN_C` | 11×11邻域高斯加权，处理局部变化 |
| 合成 | `cv2.min(otsu, adaptive)` | 两种结果取较小值，保留最清晰的文字 |
| 反色 | `cv2.bitwise_not()` | 将黑字转为白字便于后续处理 |

## 3. 精确裁剪 - `_tighten_name_crop()`

目的：去除名称周围的边框、装饰、星星等干扰，只保留名称文字

**处理步骤：**

```
1. 构建二值化图像（使用_build_letter_binary）

2. 提取前景（反色二值化）并进行阈值处理
   foreground = cv2.bitwise_not(binary)
   threshold(foreground, 180) → strong_foreground

3. 形态学操作：
   - 开运算(2×2)：去除小噪声/星星
   - 闭运算(5×1)：连接水平分散的文字

4. 找出最小外接矩形（boundingRect）

5. 智能调整垂直范围：
   - 计算ROI中每行的非零像素数
   - 設定有效行阈值 = max(5, int(宽度 * 0.08))
   - 只保留像素数 >= 阈值的行（排除稀疏干扰）

6. 应用边距和最小高度约束：
   - 水平边距：±4像素
   - 最小高度：18像素
   
7. 返回裁剪后的图像和二值化对应部分
```

## 4. 文字掩码提取 - `_extract_text_mask()`

目的：从二值化图像中只保留有效的文字像素，去除小噪点和线条

**处理步骤：**

```
1. 反色处理：将黑字转为白色前景

2. 阈值处理：threshold(foreground, 180) → 白色文字像素

3. 形态学清理：
   - 开运算(2×2)：去除 < 4像素的噪点
   - 闭运算(3×1)：连接相邻文字

4. 连通域分析：找出所有独立文字块
   - 过滤面积 < 10像素的成分（噪声）
   - 过滤宽度 < 2或高度 < 5的成分（线条）

5. 返回清理后的文字掩码
```

## 5. 多变体生成 - `_prepare_ocr_images()`

目的：生成多个图像变体供OCR引擎尝试（投票机制），提升识别鲁棒性

**生成的变体包括：**

### 基础变体
| 顺序 | 变体类型 | 方法 | 用途 |
|------|--------|------|------|
| 1 | 原始二值化 | `_build_letter_binary()` | 标准黑底白字 |
| 2 | CLAHE对比增强 | `cv2.createCLAHE()` | 改善暗背景对比度 |
| 3 | 颜色掩码 | 白色距离筛选 | 针对白字卡 |
| 4 | 文字掩码反色 | `_extract_text_mask()` | 白色背景的文字 |
| 5 | OTSU全局二值 | 自适应阈值 | 不同亮度适应 |
| 6 | CLAHE+OTSU二值 | 结合增强 | 高对比度场景 |
| 7 | 自适应阈值 | 15×15高斯邻域 | 光线不均场景 |
| 8 | CLAHE+自适应 | 双重增强 | 综合性能 |

### 尺度变体

部分变体会生成放大版本以提升识别准度：

| 条件 | 放大倍数 | 原因 |
|------|---------|------|
| 高度 ≤ 32像素 | 2.0倍 (`INTER_CUBIC`) | 细小文字超分辨率 |
| 高度 ≤ 24像素 | 3.0倍 (`INTER_CUBIC`) | 极小文字详细恢复 |

### 去重机制
- 相同的图像不会重复添加
- 限制总变体数：最多14个（防止计算过度）

### 颜色距离计算（针对白字卡）

```python
# RGB色彩空间中的欧氏距离
dist_white = sqrt( (R-255)² + (G-255)² + (B-255)² )

# 选择标准：
# - 距离白色 <= 92  → 白字掩码
```

然后对掩码应用多次形态学操作以提取完整的文字区域。

---

## 代码调用链

```
NameScanner.scan(image)
  ├─ _match_name_band(image)          # 定位名称条带
  ├─ _tighten_name_crop(image)        # 精确裁剪
  │   └─ _build_letter_binary()       # 生成二值化
  ├─ _prepare_ocr_images()            # 生成多变体
  │   ├─ _build_letter_binary()
  │   ├─ CLAHE增强
  │   ├─ 颜色掩码提取
  │   ├─ _extract_text_mask()         # 文字掩码
  │   ├─ OTSU二值化
  │   ├─ 自适应阈值
  │   └─ 尺度放大 (2x/3x)
  └─ ocr_model.ocr_preprocessed()     # 执行OCR
```

## 关键参数参考表

| 参数 | 值 | 所在函数 | 说明 |
|------|---|---------|------|
| 名称条带高度 | 26像素 | `_match_name_band` | 最大裁剪高度 |
| 权重分布 | [0.35,0.55,0.8,1.0,1.0,1.0,0.8,0.55,0.35] | `_match_name_band` | 高斯型权重曲线 |
| 强前景阈值 | 180 | `_tighten_name_crop` | 分离文字与背景 |
| 开运算核 | 2×2 | `_tighten_name_crop` | 去除小噪声 |
| 闭运算核 | 5×1 | `_tighten_name_crop` | 连接水平文字 |
| 有效行像素阈值 | max(5, width×0.08) | `_tighten_name_crop` | 排除稀疏干扰 |
| 最小高度 | 18像素 | `_tighten_name_crop` | 保证完整文字 |
| 水平边距 | ±4像素 | `_tighten_name_crop` | 保留文字两侧空间 |
| 白色距离阈值 | 92 | `_prepare_ocr_images` | RGB空间中的欧氏距离 |
| 噪点最小面积 | 10像素 | `_extract_text_mask` | 过滤连通域大小 |
| 线条最小尺寸 | 宽≥2, 高≥5 | `_extract_text_mask` | 过滤细小成分 |
| CLAHE裁剪限制 | 2.0 | `_prepare_ocr_images` | 对比度增强强度 |
| CLAHE网格 | 4×4 | `_prepare_ocr_images` | 分块处理网格 |
| 自适应阈值邻域 | 11×11/15×15 | `_build_letter_binary/_prepare_ocr_images` | 局部处理范围 |
| 最大变体数 | 14个 | `_prepare_ocr_images` | OCR投票样本数 |

## 设计理念

1. **多角度覆盖**：通过不同的二值化、增强方法生成多个变体
2. **投票机制**：OCR引擎对多个变体分别识别，选择最优结果
3. **尺度自适应**：针对小文字进行放大处理，改善识别精度
4. **噪声抑制**：通过形态学操作去除卡牌背景中的装饰元素
5. **颜色优化**：针对常见的白字舰娘卡进行颜色掩码提取
6. **去重高效**：避免冗余计算，限制变体总数

